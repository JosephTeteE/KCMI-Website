# KCMI platform — local environment (NAMES ONLY, safe placeholders)

# --- Public / browser-safe ---
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key-here
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your-turnstile-site-key-here

# --- Server-only (never expose to the browser; never prefix with NEXT_PUBLIC_) ---
SUPABASE_SECRET_KEY=your-secret-key-here
TURNSTILE_SECRET_KEY=your-turnstile-secret-key-here

# Hosted environments: staging | production
# Omit for local development. Do not set staging/production for unit tests.
# KCMI_ENVIRONMENT=staging

# Explicit seed CMS (tests/demo only). Forbidden when KCMI_ENVIRONMENT is staging or production.
# CONTENT_SOURCE=seed

# First-party Prayer intake (Care P2). Default OFF. Set to 1 only when HUMAN enables cutover testing.
# Google Form remains the visitor CTA until links are replaced in a later approved cutover.
# KCMI_PRAYER_INTAKE_ENABLED=1

# First-party Pastoral Care intake (Care P3). Default OFF.
# KCMI_PASTORAL_INTAKE_ENABLED=1

# First-party Welfare intake (Care P3). Default OFF.
# KCMI_WELFARE_INTAKE_ENABLED=1

# Website Contact → Hub Messages & Requests staff email (Resend, server-only).
# Required for live notification after a Contact form is saved. DB is system of record.
# RESEND_API_KEY=your-resend-api-key-here
# KCMI_CONTACT_NOTIFICATION_TO=contact@kcmi-rcc.org
# KCMI_CONTACT_NOTIFICATION_FROM=KCMI Website <no-reply@auth.kcmi-rcc.org>

# Local CLI mapping (`npx supabase status -o env`), names only:
#   API_URL          → NEXT_PUBLIC_SUPABASE_URL
#   PUBLISHABLE_KEY  → NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
#   SECRET_KEY       → SUPABASE_SECRET_KEY
